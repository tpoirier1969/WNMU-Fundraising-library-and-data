# Notes for v0.5.0

- Program detail now uses a popup/modal workflow instead of a static right-side panel.
- The list still reads from `pledge_program_library_summary`.
- Detail still reads directly from the base tables so import quality can be inspected.
- Add/edit buttons remain placeholders until the next functional pass.
- UI defaults to active titles and still allows archived titles from the same view.
