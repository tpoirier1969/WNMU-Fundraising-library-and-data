# v0.20.20

- Added Create + link actions for unmatched import rows so a new pledge-library title can be created directly from import review.
- Added an Existing unlinked imported airings table in Imports to expose already-imported rows with no program link.
- Tightened fundraiser list density in Scheduling and added broadcast/online/mail/grand-total fundraiser money fields.
- Updated visible version and cache-busters to v0.20.20.
