# v0.20.19

- Added an Unmatched Rows review table to the Imports workspace.
- Unmatched report rows can now be manually matched to an existing pledge-library title before import.
- Import messaging now makes it explicit that reimporting corrected reports is safe because duplicate airings are skipped automatically.
- Updated visible version and cache-busters to v0.20.19.
