(() => {
'use strict';
const A=globalThis.WNMUOneSheetAnalysis,S=globalThis.WNMUProgrammingStrategyAnalysis,cfg=globalThis.PLEDGE_MANAGER_CONFIG||{};
const state={client:null,schedules:[],airings:[],library:[],indexes:null,overrides:[],selectedScheduleId:'',analysisReady:false};
const $=s=>document.querySelector(s),esc=v=>String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#39;');
function fmt(v,year=true){const d=S.parseDate(v);return d?d.toLocaleDateString(undefined,{month:'short',day:'numeric',year:year?'numeric':undefined}):esc(v||'—');}
function clock(m){m=Number(m);if(!Number.isFinite(m))return'—';m=((m%1440)+1440)%1440;const h=Math.floor(m/60),mi=m%60;return`${h%12||12}${mi?`:${String(mi).padStart(2,'0')}`:''} ${h>=12?'PM':'AM'}`;}
function status(msg,tone=''){const n=$('#strategy-status');if(n){n.textContent=msg||'';n.className=`strategy-status${tone?` ${tone}`:''}`;}}
function makeClient(){if(!globalThis.supabase?.createClient)throw new Error('Supabase library did not load.');if(!cfg.SUPABASE_URL||!cfg.SUPABASE_ANON_KEY)throw new Error('Supabase configuration is missing.');return globalThis.supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_ANON_KEY);}
async function requireAdmin(){state.client=makeClient();const{data,error}=await state.client.auth.getSession();if(error)throw error;const session=data?.session||null,email=String(session?.user?.email||'').trim().toLowerCase(),admins=Array.isArray(cfg.ADMIN_EMAILS)?cfg.ADMIN_EMAILS.map(x=>String(x).trim().toLowerCase()).filter(Boolean):[],ok=!!(session&&(!admins.length||admins.includes(email)));if(ok){$('#strategy-role').textContent=email?`Admin · ${email}`:'Admin';return true;}$('#strategy-app')?.classList.add('hidden');const gate=$('#strategy-access-gate');if(gate){gate.classList.remove('hidden');gate.innerHTML=`<div class="report-gate-card"><div class="report-kicker">Admin report center</div><h1>Admin access required</h1><p>${esc(session?`${email||'This account'} does not have administrator report access.`:'Sign in as an administrator from the Pledge Program Library, then return to this report.')}</p><a class="report-button primary" href="./">Open Pledge Program Library</a></div>`;}return false;}
async function fetchAll(table,select='*',order=''){const rows=[];for(let from=0;;from+=1000){let q=state.client.from(table).select(select).range(from,from+999);if(order)q=q.order(order,{ascending:true});const{data,error}=await q;if(error)throw error;const chunk=Array.isArray(data)?data:[];rows.push(...chunk);if(chunk.length<1000)break;}return rows;}
async function fetchOptional(table){try{return await fetchAll(table);}catch(e){console.warn(`Optional report source ${table} unavailable.`,e);return[];}}
function todayStart(){const d=new Date();d.setHours(0,0,0,0);return d;}
function defaultSchedule(){const today=todayStart();return state.schedules.find(x=>S.parseDate(x.endDate)>=today)||state.schedules.at(-1)||null;}
function scheduleLabel(s){const upcoming=S.parseDate(s.startDate)>=todayStart();return`${s.title} · ${fmt(s.startDate)}–${fmt(s.endDate,false)} · ${upcoming?'Upcoming':'Past'}`;}
async function loadSchedules(){status('Loading fundraiser choices…');const rows=await fetchAll('pledge_fundraiser_schedules','id,title,start_date,end_date,created_at,updated_at,schedule_data','start_date');state.schedules=A.prepareSchedules(rows.map(A.normalizeSchedule)).filter(x=>x.startDate&&x.endDate&&!x.reportOnly).sort((a,b)=>String(a.startDate).localeCompare(String(b.startDate)));renderControls();status('Fundraiser choices ready. Loading history and Program Library…');}
async function loadAnalysisData(){const[airings,library,overrides]=await Promise.all([fetchAll('pledge_program_airings_v2','*','air_date'),fetchAll('pledge_programs_v2'),fetchOptional('pledge_program_editorial_overrides')]);state.airings=A.canonicalizeImportedAirings?A.canonicalizeImportedAirings(airings):airings;state.library=library;state.indexes=A.buildLibraryIndexes(library);state.overrides=overrides;state.analysisReady=true;status('Data loaded. Calculating strategy…');renderStrategy();}
function renderControls(){const select=$('#strategy-fundraiser');if(!select)return;select.innerHTML=state.schedules.map(x=>`<option value="${esc(x.id)}">${esc(scheduleLabel(x))}</option>`).join('');const initial=defaultSchedule();if(initial){state.selectedScheduleId=initial.id;select.value=initial.id;}select.disabled=false;select.onchange=()=>{state.selectedScheduleId=select.value;renderStrategy();};$('#strategy-print').onclick=()=>window.print();}
const selectedSchedule=()=>state.schedules.find(x=>String(x.id)===String(state.selectedScheduleId))||null;
function evidenceFor(schedule){const cutoff=S.evidenceCutoff(schedule),cutoffAirings=S.filterEvidenceAirings(state.airings,cutoff),authoritative=state.schedules.filter(x=>String(x.id)!==String(schedule.id)&&S.parseDate(x.startDate)<=S.parseDate(cutoff)),reportOnly=typeof A.reportOnlySchedulesFromAirings==='function'?A.reportOnlySchedulesFromAirings(authoritative,cutoffAirings,state.indexes):[],history=[...authoritative,...reportOnly].filter(x=>S.parseDate(x.startDate)<=S.parseDate(cutoff)),analyses=history.map(x=>A.analyzeSchedule(x,cutoffAirings,state.indexes));return A.historicalRows(analyses);}
function topicPill(x){return`<span class="strategy-pill"><strong>${esc(x.topic)}</strong><small>${esc(x.confidence||'')}</small></span>`;}
function recommendationHtml(x){const flags=[];if(x.local)flags.push('Local / U.P.');if(x.season?.holiday&&x.season.targetSeason==='December')flags.push('Seasonal fit');if(x.drama?.currentCycle)flags.push('Current Drama Doc');if(x.programmer?.rating)flags.push(`Programmer: ${x.programmer.label}`);return`<article class="strategy-title-card"><div class="strategy-title-card-head"><div><strong>${esc(x.title)}</strong><span>${esc(x.topic)}${x.secondary?` · ${esc(x.secondary)}`:''}</span></div><div class="strategy-score"><b>${Math.round(x.score)}</b><small>${esc(x.fit)}</small></div></div><div class="strategy-title-meta">Confidence: ${esc(x.confidence)}${flags.length?` · ${flags.map(esc).join(' · ')}`:''}</div><p>${esc(x.reasons.slice(0,2).join(' '))}</p>${x.cautions.length?`<div class="strategy-caution">${esc(x.cautions.slice(0,2).join(' '))}</div>`:''}</article>`;}
function topicComparisonSection(strategy){const rows=strategy.topicComparison||[];if(!rows.length)return'<section class="sheet-section"><h2>Topic comparison</h2><p>No eligible topic data is available.</p></section>';return`<section class="sheet-section"><div class="strategy-section-head"><div><h2>Topic comparison</h2><p>Every topic currently represented by an eligible Library title. This is WNMU evidence, not a quota or percentage mix.</p></div></div><div class="strategy-topic-list">${rows.map(x=>`<div class="strategy-topic-list-row"><div><strong>${esc(x.topic)}</strong><span class="strategy-strength strength-${esc(x.signal.toLowerCase().replace(/\s+/g,'-'))}">${esc(x.signal)}</span></div><div>${x.historyRows?`${x.historyRows} rate-valid historical row${x.historyRows===1?'':'s'}${Number.isFinite(x.medianRate)?` · median $${Math.round(x.medianRate)}/hr`:''}`:'No rate-valid WNMU history yet'}</div><div>${x.bestWindows.length?`Best supported: ${x.bestWindows.map(w=>`${w.weekday} ${w.label}`).join(' · ')}`:'No established weekday/time fit yet'}</div>${x.subtopics.length?`<small>Current subtopics: ${esc(x.subtopics.join(' · '))}</small>`:''}</div>`).join('')}</div></section>`;}
function groupedDayparts(strategy){const groups=new Map();for(const slot of strategy.windows.filter(x=>!x.experimental&&!x.blocked)){const key=`${slot.weekday}|${slot.label}`,g=groups.get(key)||{weekday:slot.weekday,label:slot.label,rows:0,rates:[]};g.rows+=slot.evidenceRows;for(const item of slot.recommendations.slice(0,3))if(item.dayHistory?.rates?.length)g.rates.push(...item.dayHistory.rates);groups.set(key,g);}return[...groups.values()].map(g=>({...g,median:S.median(g.rates)})).sort((a,b)=>(b.median||0)-(a.median||0)||b.rows-a.rows);}
function daypartSection(strategy){const strengths=groupedDayparts(strategy),experiments=strategy.windows.filter(x=>x.experimental);return`<section class="sheet-section strategy-two-column"><div><h2>Day/time evidence</h2>${strengths.map(x=>`<div class="strategy-line"><strong>${esc(x.weekday)} ${esc(x.label)}</strong><span>${x.rows?`${x.rows} exact-weekday comparable historical row${x.rows===1?'':'s'}${Number.isFinite(x.median)?` · median about $${Math.round(x.median)}/hr`:''}`:'No exact-weekday evidence'}</span></div>`).join('')||'<p>No normal-window evidence available.</p>'}</div><div><h2>Experimental opportunities</h2>${experiments.map(x=>`<div class="strategy-line experimental"><strong>${esc(x.weekday)} ${fmt(x.date,false)} · ${clock(x.startMinutes)}–${clock(x.endMinutes)}</strong><span>Experimental slot. Recommendations here are intentionally separated from established day/time patterns.</span></div>`).join('')||'<p>No experimental window identified.</p>'}</div></section>`;}
function dayMapSection(strategy){const byDate=new Map();for(const slot of strategy.windows){if(!byDate.has(slot.date))byDate.set(slot.date,[]);byDate.get(slot.date).push(slot);}return`<section class="sheet-section"><h2>Day-by-day programming map</h2><div class="strategy-days">${[...byDate.entries()].map(([date,slots])=>`<section class="strategy-day"><header><div><strong>${esc(slots[0].weekday)}</strong><span>${fmt(date)}</span></div></header>${slots.map(slot=>`<div class="strategy-slot ${slot.experimental?'is-experimental':''} ${slot.blocked?'is-blocked':''}"><div class="strategy-slot-head"><div><strong>${clock(slot.startMinutes)}–${clock(slot.endMinutes)} · ${esc(slot.label)}</strong><span class="strategy-slot-badge">${slot.blocked?'Protected / unavailable':slot.experimental?'Experimental opportunity':'Normal pledge window'}</span></div><small>${slot.blocked?'Not pledge inventory':slot.evidenceRows?`${slot.evidenceRows} exact-weekday comparable historical row${slot.evidenceRows===1?'':'s'}`:'No exact-weekday slot history'}</small></div>${slot.blocked?'<p class="strategy-blocked-note">Friday 8–9 PM is intentionally excluded from pledge recommendations.</p>':`<div class="strategy-topic-row"><span>Supported topics</span>${slot.strongestTopics.length?slot.strongestTopics.map(topicPill).join(''):'<em>No established topic fit for this exact day/time</em>'}</div>${slot.alternativeTopics.length?`<div class="strategy-topic-row secondary"><span>Other supported</span>${slot.alternativeTopics.map(topicPill).join('')}</div>`:''}<div class="strategy-title-grid">${slot.recommendations.slice(0,4).map(recommendationHtml).join('')||'<p>No eligible Program Library title for this slot.</p>'}</div>`}</div>`).join('')}</section>`).join('')}</div></section>`;}
function compact(items,renderer,empty){return items?.length?`<div class="strategy-compact-list">${items.map(renderer).join('')}</div>`:`<p>${esc(empty)}</p>`;}
function supportingSections(strategy){return`<section class="sheet-section strategy-two-column"><div><h2>Repeat candidates</h2>${compact(strategy.repeats,x=>`<div><strong>${esc(x.title)}</strong><span>${esc(x.topic)} · score ${Math.round(x.score)} · supported on ${x.slots.length} separated prime windows.</span></div>`,'No repeat candidate clears the threshold.')}<h2>Seasonal opportunities</h2>${compact(strategy.seasonal,x=>`<div><strong>${esc(x.title)}</strong><span>${esc(x.topic)} · ${esc(x.season.notes.join(' ')||`${x.season.targetSeason} seasonal support`)}</span></div>`,'No distinct seasonal opportunity identified.')}</div><div><h2>Local / U.P. opportunities</h2>${compact(strategy.local,x=>`<div><strong>${esc(x.title)}</strong><span>${esc(x.topic)} · best-window score ${Math.round(x.score)} · ${esc(x.fit)}</span></div>`,'No eligible Local / U.P. title identified.')}<h2>Titles to avoid / rest</h2>${compact(strategy.avoid,x=>`<div><strong>${esc(x.title)}</strong><span>${esc(x.reasons.join(' · '))}</span></div>`,'No title needs a prominent rest/avoid caution.')}</div></section>`;}
function rightsSection(strategy){const desc=x=>`${x.rightsStart?`Starts ${fmt(x.rightsStart)}`:''}${x.rightsStart&&x.rightsEnd?' · ':''}${x.rightsEnd?`Ends ${fmt(x.rightsEnd)}`:''}`;return`<section class="sheet-section strategy-two-column"><div><h2>Rights constraints</h2>${compact(strategy.rights.unavailable.slice(0,20),x=>`<div><strong>${esc(x.title)}</strong><span>${esc(desc(x))}</span></div>`,'No fully unavailable title detected.')}</div><div><h2>Partial-drive rights</h2>${compact(strategy.rights.partial.slice(0,20),x=>`<div><strong>${esc(x.title)}</strong><span>${esc(desc(x))}</span></div>`,'No partial-drive rights restriction detected.')}</div></section>`;}
function limitationsSection(strategy){return`<section class="sheet-section"><h2>Evidence confidence & limitations</h2><div class="strategy-facts"><div><strong>${strategy.evidenceRows.toLocaleString()}</strong><span>pre-cutoff historical program rows</span></div><div><strong>${strategy.evidenceFundraisers.toLocaleString()}</strong><span>historical fundraiser/event groups</span></div></div><ul class="strategy-limitations">${strategy.limitations.map(x=>`<li>${esc(x)}</li>`).join('')}<li>${esc(strategy.peerEvidence.note)}</li></ul></section>`;}
function renderStrategy(){const schedule=selectedSchedule(),out=$('#strategy-output');if(!schedule||!out){if(out)out.innerHTML='<div class="report-empty">No saved fundraiser is available.</div>';return;}if(!state.analysisReady){out.innerHTML='<div class="report-loading-card"><strong>Fundraiser selected.</strong><span>Loading WNMU history and Program Library data for analysis…</span></div>';return;}status('Calculating pre-drive strategy…');try{const strategy=S.buildStrategy({schedule,library:state.library,evidenceRows:evidenceFor(schedule),overrides:state.overrides,now:new Date()});out.innerHTML=`<article class="report-sheet strategy-sheet"><header class="sheet-title"><div><div class="report-kicker">WNMU-TV PBS pre-drive planning</div><h1>Fundraiser Programming Strategy</h1><p>${esc(schedule.title)} · ${fmt(schedule.startDate)}–${fmt(schedule.endDate,false)}</p></div><div class="sheet-stamp">Evidence through ${fmt(strategy.cutoff)}</div></header><section class="strategy-summary"><div><span>Evidence through</span><strong>${fmt(strategy.cutoff)}</strong><small>Future drives are capped at today; historical drives stop before they began.</small></div></section>${topicComparisonSection(strategy)}${daypartSection(strategy)}${dayMapSection(strategy)}${supportingSections(strategy)}${rightsSection(strategy)}${limitationsSection(strategy)}</article>`;status(`Strategy generated using evidence through ${fmt(strategy.cutoff)}.`,'good');}catch(e){console.error(e);out.innerHTML=`<div class="report-empty"><strong>Could not generate strategy.</strong><p>${esc(e?.message||e)}</p></div>`;status('Strategy generation failed.','error');}}
async function init(){try{if(!A||!S)throw new Error('Programming strategy analysis modules did not load.');if(!await requireAdmin())return;await loadSchedules();renderStrategy();await loadAnalysisData();}catch(e){console.error(e);status(e?.message||String(e),'error');const out=$('#strategy-output');if(out)out.innerHTML=`<div class="report-empty"><strong>Report could not start.</strong><p>${esc(e?.message||e)}</p></div>`;}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else void init();
})();