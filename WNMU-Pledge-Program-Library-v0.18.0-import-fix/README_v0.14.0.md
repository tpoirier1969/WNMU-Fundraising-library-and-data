# WNMU Pledge Program Library v0.14.0

Revision focus:
- NOLA-first report matching
- import only airing-level report facts
- derive fundraiser totals instead of storing money twice
- scheduler copy/paste cleanup (modal copy/paste now closes; desktop right-click menu added)

Run the SQL revision in `supabase/report_import_revision_v0.14.0.sql`, then publish the site files.
