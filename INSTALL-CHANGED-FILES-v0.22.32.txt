Install changed files for v0.22.32

Copy these files into the matching paths in your existing WNMU Pledge Program Library folder:

- index.html
- version.json
- assets/js/core.js
- assets/js/ui-imports.js

Do not replace config.js. This patch does not include config.js.

After copying, hard-refresh the browser so the v0.22.32 cache-busted files load.
