v0.21.99

- Live Break comparison ignores/disables the Live-break advanced filter so it can never filter out the live rows and report a false $0 result.
- Performance Analytics live-break logic now reads only canonical saved Scheduling placement liveBreakFlag/liveBreakNotes, not imported live_break fallback fields.
- Seasonal lift graph uses the full available analytics width; the plain-English read moves below it so the four season columns are no longer hidden behind the read panel.
- Version bumped to v0.21.99.
- No config.js included.
